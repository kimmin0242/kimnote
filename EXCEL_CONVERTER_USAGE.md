# Excel 데이터 변환 도구 - 빠른 시작 가이드

## 🎯 목적

사용자의 요청: **"2번을 먼저 해주시면 엑셀에 새로운 시트로 넣어볼깨요"**

가로 형식의 복잡한 부품 데이터를 **차종별 5개 시트**로 분리하고 **세로 형식**으로 정규화합니다.

## 📊 변환 결과

### 입력 형식 (1개 시트, 가로)
```
제조사 | 대분류 | 소분류 | 모델명 | 세대 | 연식 | 연료 | 엔진 | 엔진오일(대) | 미션오일 | ... [30+ 컬럼]
```

### 출력 형식 (5개 시트, 세로)
```
1️⃣ 제너시스_세단 (G90, G80, G70)
2️⃣ 제너시스_SUV (GV60, GV70, GV80, GV90)  
3️⃣ 현대_승용차 (아반떼, 쏘나타, 그랜저)
4️⃣ 현대_SUV (코나, 투싼, 싼타페, 팰리세이드)
5️⃣ 현대_친환경차 (아이오닉5, 아이오닉6, 넥쏘)
```

각 시트의 컬럼:
```
제조사 | 브랜드 | 카테고리 | 모델명 | 세대 | 연식 | 연료타입 | 엔진타입 | 부품분류 | 부품타입 | 부품번호 | 용량 | 수량 | 옵션 | 비고
```

## 🚀 사용 방법

### 방법 1: 웹 인터페이스 (추천)

1. **관리자 페이지 접속**
   ```
   http://your-domain.com/admin/
   ```

2. **"Excel 변환 도구" 카드 클릭**

3. **파일 업로드**
   - Excel 파일(.xlsx)을 드래그 앤 드롭
   - 또는 클릭하여 파일 선택

4. **"변환 시작" 버튼 클릭**

5. **변환 완료 후 다운로드**
   - 통계 확인 (각 시트별 부품 수)
   - "변환된 파일 다운로드" 버튼 클릭

### 방법 2: 명령줄

```bash
# Python 모듈 설치 (최초 1회)
pip3 install openpyxl

# 변환 실행
cd /volume1/web/hyundai-parts/
python3 excel_converter_method2.py 원본파일.xlsx

# 결과: 원본파일_차종별시트_20251122_143025.xlsx
```

## ✨ 주요 기능

### 1. 자동 차종 분류
- **제조사** + **카테고리** + **모델명** 조합으로 자동 분류
- 제네시스/현대 자동 구분
- 세단/SUV/친환경차 자동 분류

### 2. 부품 옵션 자동 분리
**입력:**
```
[고급형/활성탄] 97133-D3000, [일반형] 97133-C1000
```

**출력:**
```
행1: 부품번호=97133-D3000, 옵션=고급형/활성탄
행2: 부품번호=97133-C1000, 옵션=일반형
```

### 3. 부품 분류 자동 매핑
```
엔진오일(대) → 오일 및 액체류
에어필터 → 필터류
브레이크 패드 → 제동류
배터리 → 전장 및 기타 부품류
```

### 4. Excel 스타일링
- ✅ 헤더 행 고정 (스크롤 시 항상 표시)
- ✅ 파란색 헤더 배경, 흰색 텍스트
- ✅ 컬럼 너비 자동 조정
- ✅ 모든 셀 테두리 적용

## 📈 변환 통계 예시

```
✅ 변환 완료!

📊 변환 통계:
  총 처리 차량: 145개
  건너뛴 행: 3개

  시트별 부품 수:
    제너시스_세단: 234개
    제너시스_SUV: 189개
    현대_승용차: 567개
    현대_SUV: 445개
    현대_친환경차: 123개

  총 부품 레코드: 1,558개
```

## 📝 입력 파일 요구사항

### 필수 컬럼 (헤더 정확히 일치 필요)
```
제조사
대분류
소분류
모델명
상세 트림/세대
연식
동력원 유형
세부 엔진/동력계
```

### 부품 컬럼 (예시)
```
엔진오일(대), 엔진오일(소)
미션오일, 브레이크오일
에어필터, 오일필터, 에어컨필터(실내)
배터리, 점화플러그
...
```

### 파일 형식
- ✅ `.xlsx` (Excel 2007 이상)
- ❌ `.xls` (구버전 Excel)

## ⚡ 빠른 테스트

샘플 파일로 테스트:

```bash
# 샘플 파일 생성
cd /volume1/web/hyundai-parts/
python3 create_sample_excel.py

# 변환 실행
python3 excel_converter_method2.py sample_parts_data.xlsx test.xlsx

# 결과 확인
# test.xlsx 파일 열기
```

## 🔧 문제 해결

### "openpyxl 모듈 없음" 오류
```bash
pip3 install openpyxl
```

### 파일 업로드 크기 제한
PHP 설정 수정 (`php.ini`):
```ini
upload_max_filesize = 50M
post_max_size = 50M
```

### 차종이 잘못 분류됨
`excel_converter_method2.py` 파일 내 `VEHICLE_TYPE_SHEETS` 딕셔너리 수정

## 📂 생성되는 파일

웹 인터페이스 사용 시:
```
/volume1/web/hyundai-parts/
├── converted_files/
│   └── 차종별시트_20251122_143025.xlsx  ← 다운로드 가능
└── temp_uploads/                         ← 자동 삭제됨
```

명령줄 사용 시:
```
/volume1/web/hyundai-parts/
└── 원본파일명_차종별시트_20251122_143025.xlsx
```

## 🎓 다음 단계

1. ✅ **Excel 파일 변환 완료**
2. 📥 각 시트의 데이터 검토 및 조정
3. 🔄 데이터베이스 임포트 (향후 개발 예정)
4. 🎯 부품 매핑 시스템에서 활용

## 📞 참고 자료

- 상세 문서: `EXCEL_CONVERTER_README.md`
- 샘플 생성: `create_sample_excel.py`
- Python 스크립트: `excel_converter_method2.py`
- 웹 인터페이스: `/admin/` → "Excel 변환 도구"

---

**완료 일자:** 2025-11-22  
**요청 사항:** "2번을 먼저 해주시면 엑셀에 새로운 시트로 넣어볼깨요" ✅ 완료
