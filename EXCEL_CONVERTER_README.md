# Excel 데이터 변환 도구 - 사용 설명서

## 📋 개요

현대차/제네시스 부품 데이터를 **가로 형식**에서 **세로 형식**으로 변환하고, **차종별 시트로 분리**하는 도구입니다.

## 🎯 변환 방법

### 방법 2: 차종별 시트 분리 (현재 구현됨)

**입력 형식 (가로)**:
```
제조사 | 대분류 | 소분류 | 모델명 | 세대 | 연식 | 연료타입 | 엔진 | 엔진오일(대) | 미션오일 | ... [30+ 컬럼]
```

**출력 형식 (세로, 5개 시트로 분리)**:

#### 생성되는 시트

1. **제너시스_세단**
   - G90, G80, G70 등 제네시스 브랜드 세단 모델

2. **제너시스_SUV**
   - GV60, GV70, GV80, GV90 등 제네시스 브랜드 SUV 모델

3. **현대_승용차**
   - 아반떼, 벨로스터, 쏘나타, 그랜저, 에쿠스, i30, i40 등

4. **현대_SUV**
   - 코나, 투싼, 싼타페, 팰리세이드, 베뉴, 넥쏘, 맥스크루즈 등

5. **현대_친환경차**
   - 아이오닉5, 아이오닉6, 넥쏘, GV60 등 전기차/수소차

#### 각 시트의 컬럼 구조

```
제조사 | 브랜드 | 카테고리 | 모델명 | 세대 | 연식 | 연료타입 | 엔진타입 | 부품분류 | 부품타입 | 부품번호 | 용량(L) | 수량 | 옵션 | 비고
```

## 🚀 사용 방법

### 방법 1: 웹 인터페이스 (추천)

1. **관리자 페이지 접속**
   ```
   http://your-domain.com/admin/
   ```

2. **Excel 변환 도구 클릭**
   - 대시보드에서 "Excel 변환 도구" 카드 클릭

3. **파일 업로드**
   - 드래그 앤 드롭 또는 클릭하여 파일 선택
   - `.xlsx` 형식만 지원

4. **변환 시작**
   - "변환 시작" 버튼 클릭
   - 변환 완료 후 자동으로 다운로드 링크 제공

5. **결과 다운로드**
   - 변환된 파일 다운로드
   - 각 시트별 통계 확인

### 방법 2: 명령줄 인터페이스

```bash
# Python 모듈 설치 (최초 1회만)
pip3 install openpyxl

# 변환 실행
cd /volume1/web/hyundai-parts/
python3 excel_converter_method2.py 입력파일.xlsx

# 또는 출력 파일명 지정
python3 excel_converter_method2.py 입력파일.xlsx 출력파일.xlsx
```

## 📊 데이터 처리 로직

### 1. 차종 분류 기준

스크립트는 다음 정보를 조합하여 차종을 자동 분류합니다:

- **제조사** (현대, 제네시스)
- **카테고리** (세단, SUV, 전기차 등)
- **모델명** (G80, 코나, 아이오닉5 등)

### 2. 부품 옵션 처리

부품번호 셀에 여러 옵션이 있는 경우 자동으로 분리합니다:

**입력 예시**:
```
[고급형/활성탄] 97133-D3000, [일반형] 97133-C1000
```

**출력 결과**:
```
행1: 부품번호=97133-D3000, 옵션=고급형/활성탄
행2: 부품번호=97133-C1000, 옵션=일반형
```

### 3. 부품 분류 자동 매핑

각 부품 타입을 대분류로 자동 분류합니다:

| 부품 타입 | 부품 분류 |
|-----------|-----------|
| 엔진오일(대), 엔진오일(소), 미션오일 등 | 오일 및 액체류 |
| 에어필터, 오일필터, 에어컨필터 등 | 필터류 |
| 브레이크 패드, 브레이크 디스크, 타이어 등 | 제동류 |
| 배터리, 점화플러그, 와이퍼 등 | 전장 및 기타 부품류 |

### 4. 빈 값 처리

다음 값들은 자동으로 제외됩니다:
- 빈 셀
- `/` (슬래시)
- `-` (하이픈)
- `N/A`

## 📁 파일 구조

```
/volume1/web/hyundai-parts/
├── excel_converter_method2.py          # Python 변환 스크립트
├── admin_excel_converter.php           # 웹 인터페이스
├── admin_excel_converter_api.php       # API 백엔드
├── requirements_excel.txt              # Python 의존성
├── temp_uploads/                       # 업로드된 파일 임시 저장
└── converted_files/                    # 변환된 파일 저장
```

## 🔧 시스템 요구사항

### Python 환경
- Python 3.6 이상
- openpyxl 모듈

### PHP 환경
- PHP 8.0 이상
- exec() 함수 사용 가능
- 파일 업로드 권한

### 설치

```bash
# Python 모듈 설치
pip3 install -r requirements_excel.txt

# 또는 직접 설치
pip3 install openpyxl
```

## 📈 변환 통계 예시

```
✅ 변환 완료!

📊 변환 통계:
  총 처리 차량: 145개
  건너뛴 행: 3개

  시트별 부품 수:
    제너시스_세단: 234개
    제너시스_SUV: 189개
    현대_승용차: 567개
    현대_SUV: 445개
    현대_친환경차: 123개

  총 부품 레코드: 1,558개
```

## 🎨 출력 파일 특징

### 스타일링
- **헤더 행**: 파란색 배경, 흰색 글씨, 굵게
- **고정 행**: 스크롤 시 헤더 항상 표시
- **열 너비**: 자동 조정 (가독성 최적화)
- **테두리**: 모든 셀에 얇은 테두리

### 데이터 정렬
- 제조사별 → 브랜드별 → 모델별 → 세대별 정렬
- 각 차량 내에서 부품 타입별 정렬

## ⚠️ 주의사항

### 입력 파일 요구사항

1. **필수 컬럼** (헤더 이름 정확히 일치 필요):
   - 제조사
   - 대분류
   - 소분류
   - 모델명
   - 상세 트림/세대
   - 연식
   - 동력원 유형
   - 세부 엔진/동력계

2. **부품 컬럼**:
   - 엔진오일(대), 엔진오일(소), 미션오일, 브레이크오일 등
   - 에어필터, 오일필터, 에어컨필터 등
   - 기타 부품들...

3. **파일 형식**:
   - `.xlsx` 형식만 지원 (`.xls` 구버전 미지원)
   - Excel 2007 이상에서 생성된 파일

### 데이터 정확성

- 모델명이 비어있는 행은 자동으로 건너뜁니다
- 부품번호가 없는 컬럼은 무시됩니다
- 차종 분류가 애매한 경우 기본값으로 설정됩니다

## 🔍 문제 해결

### 1. "openpyxl 모듈 없음" 오류

```bash
pip3 install openpyxl
```

### 2. 파일 업로드 크기 제한

PHP 설정 확인:
```ini
upload_max_filesize = 50M
post_max_size = 50M
```

### 3. 변환 시간이 오래 걸림

- 대용량 파일(1000+ 차량)의 경우 1-2분 소요 가능
- 명령줄 방식이 웹 방식보다 안정적

### 4. 차종이 잘못 분류됨

스크립트 내 `VEHICLE_TYPE_SHEETS` 딕셔너리를 수정하여 분류 기준 조정 가능

## 📝 다음 단계

변환된 파일을 데이터베이스에 임포트하려면:

1. 각 시트별로 데이터 검토
2. 필요시 수동 조정
3. `admin_import_data.php` 도구 사용 (추후 구현 예정)
4. 또는 직접 SQL INSERT 문 생성

## 🆕 업데이트 히스토리

### 2025-11-22
- ✅ 방법 2 (차종별 시트 분리) 구현
- ✅ 웹 인터페이스 추가
- ✅ 부품 옵션 자동 분리 기능
- ✅ 통계 표시 기능
- ✅ 드래그 앤 드롭 업로드

## 💡 팁

1. **대용량 파일**: 명령줄 방식 권장
2. **반복 작업**: 웹 인터페이스가 더 편리
3. **데이터 검증**: 변환 후 각 시트별로 샘플 확인 필수
4. **백업**: 원본 파일 항상 보관

## 📞 지원

문제가 발생하면 로그 파일 확인:
- 웹: 브라우저 콘솔 (F12)
- CLI: 터미널 출력 메시지
